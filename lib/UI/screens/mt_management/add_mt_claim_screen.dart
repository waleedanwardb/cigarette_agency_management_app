// lib/UI/screens/mt_management/add_mt_claim_screen.dart
import 'package:flutter/material.dart';

class AddMTClaimScreen extends StatelessWidget {
  const AddMTClaimScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add MT Claim'),
      ),
      body: const Center(
        child: Text('This is a placeholder for the Add MT Claim screen.'),
      ),
    );
  }
}