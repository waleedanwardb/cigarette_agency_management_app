// lib/UI/screens/vehicle/add_vehicle_expense_screen.dart
import 'package:flutter/material.dart';

class AddVehicleExpenseScreen extends StatelessWidget {
  const AddVehicleExpenseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Add Vehicle Expense'),
      ),
      body: const Center(
        child: Text('This is a placeholder for the Add Vehicle Expense screen.'),
      ),
    );
  }
}